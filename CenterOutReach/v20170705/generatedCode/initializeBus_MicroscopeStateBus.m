function bus = initializeBus_MicroscopeStateBus()
%#codegen
% DO NOT EDIT: Auto-generated by 
%   BusSerialize.writeInitializeBusCode('MicroscopeStateBus')

    coder.varsize('bus.saveBase', 200, true);
    bus.saveBase = zeros([0 1], 'uint8');
    coder.varsize('bus.saveLeaf', 200, true);
    bus.saveLeaf = zeros([0 1], 'uint8');
    bus.saveCounter = uint16(0);
    coder.varsize('bus.objective', 50, true);
    bus.objective = zeros([0 1], 'uint8');
    bus.objectiveMag = double(0);
    bus.objectiveNA = double(0);
    bus.orbitalPitch = double(0);
    bus.orbitalYaw = double(0);
    bus.scopeX = double(0);
    bus.scopeY = double(0);
    bus.scopeZ = double(0);
    bus.pmtGainGaAsP1 = double(0);
    bus.pmtGainGaAsP2 = double(0);
    bus.pmtGainLightGuide = double(0);
    bus.pockels = double(0);
    bus.pixelSizeX = double(0);
    bus.pixelSizeY = double(0);
    bus.pixelsX = double(0);
    bus.pixelsY = double(0);
    bus.galvoMode = GalvoMode.Galvo;
    bus.laserWavelength = double(0);
    bus.dwellTime = double(0);
    bus.frameRate = double(0);
    bus.cameraIsAcquiring = uint8(0);
    coder.varsize('bus.cameraRelativePath', 200, true);
    bus.cameraRelativePath = zeros([0 1], 'uint8');
    coder.varsize('bus.cameraFileShort', 200, true);
    bus.cameraFileShort = zeros([0 1], 'uint8');
    bus.cameraFileCounter = uint16(0);
    bus.mOEGLedMode = LedMode.Off;

end