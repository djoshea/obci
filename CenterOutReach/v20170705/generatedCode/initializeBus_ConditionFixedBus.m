function bus = initializeBus_ConditionFixedBus()
%#codegen
% DO NOT EDIT: Auto-generated by 
%   BusSerialize.writeInitializeBusCode('ConditionFixedBus')

    bus.enforceMinPeakVelocity = uint8(0);
    bus.fracZeroDelay = double(0);
    bus.fracShortDelay = double(0);
    bus.delayRangeShortMin = double(0);
    bus.delayRangeShortMax = double(0);
    bus.delayRangeLongMin = double(0);
    bus.delayRangeLongMax = double(0);
    bus.delayNominal = double(0);
    bus.centerX = double(0);
    bus.centerY = double(0);
    bus.targetX = double(0);
    bus.targetY = double(0);
    bus.targetSize = double(0);
    bus.targetAcceptanceSize = double(0);
    bus.targetDistance = double(0);
    bus.targetDirection = double(0);
    bus.targetDirectionName = DirectionName.None;
    bus.hasCenterTouch = uint8(1);
    bus.juiceDuringTargetHold = uint8(0);
    bus.trialCategory = TrialCategory.Unknown;
    bus.hasImaging = uint8(0);
    bus.conditionId = uint16(0);
    bus.conditionProportion = double(0);

end